const express = require('express');
const middleware = require('../middleware/index1');
const User = require("../models/user");



//create a function that will return the student with same intial 3 letter of username
